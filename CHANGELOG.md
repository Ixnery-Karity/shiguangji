# 变更日志 CHANGELOG

本项目所有重要变更均记录于此文件。
版本遵循语义化命名，最新版本在最上方。

---

## [v0.3.1] - 2026-07-31

品牌定名与开源化：项目正式定名「拾光集（ShiGuangJi）」，代码托管至 GitHub。

### 新增 Added
- **项目定名「拾光集」**：「拾」取二手好物、旧物新生；「拾光」谐音“时光”，呼应校园记忆。
- **GitHub 托管**：仓库 <https://github.com/Ixnery-Karity/shiguangji>，初始化 git 并推送全部内容。
- **`CLAUDE.md` 协作约定**：明确“每次产出同步整理文档、每次有意义更新推送 GitHub”的工作流。
- **`.gitignore`**：排除系统临时文件与 Python 缓存。

### 变更 Changed
- **`README.md`**：标题与品牌说明更新为「拾光集」，新增协作约定章节。

---

## [v0.3.0] - 2026-07-14

「从演示到真实」升级：消息实时化、邮箱验证码服务端化、交易双向确认真实化，三大核心链路接入云端真实逻辑（均保留无云环境的演示回退，零配置仍可跑通）。

### 新增 Added
- **云函数 `authEmail`**：邮箱验证码服务端生成/存储/校验。
  - 6 位验证码写入 `authCodes` 集合，5 分钟有效、60 秒发送限频、验证后一次性作废。
  - 配置 SMTP 环境变量（`SMTP_HOST/PORT/USER/PASS`）后通过 nodemailer **真实发送邮件**；未配置时返回演示码。
  - 验证通过后由服务端将用户 `authLevel` 升为 1 并绑定邮箱（已是 2 不降级）。
- **云函数 `tradeConfirm`**：交易撮合闭环服务端化。
  - `create`：以当前用户为买家创建交易（校验不能自买自卖、同商品进行中交易去重）。
  - `confirm`：按 openid 判定买家/卖家角色分别确认；**双方都确认后交易置「已完成」并自动把商品标记「已售」**。
  - `get`：按 tradeId/itemId 查询交易，供聊天页恢复交易卡片。
- **消息实时化**：`utils/db.js` 新增 `getOrCreateChat / myChats / listMessages / sendMessage / watchMessages / watchTrade`；聊天基于云端 `chats/messages` 集合真实读写，`.watch()` 实时监听双端同步。

### 变更 Changed
- **`pages/chatRoom`**：重写为「云端实时会话」模式——真实消息收发 + watch 推送渲染、打开时恢复历史消息与交易状态、交易状态 watch 实时同步对方确认；无云环境自动回退原演示对话。
- **`pages/chatList`**：接云端 `chats` 集合展示我参与的真实会话（买/卖双视角、最近消息摘要、相对时间）；无云环境回退演示会话。
- **`pages/auth`**：验证码改走 `authEmail` 云函数（服务端校验、服务端升级等级）；云函数不可用时回退本地演示码。
- **`pages/detail`**：进入聊天时携带卖家 openid（`sellerId`），支撑真实会话与交易角色判定。
- **`utils/db.js`**：`createTrade/confirmTrade` 优先走 `tradeConfirm` 云函数（服务端权限写入），新增 `getTrade`、`sendEmailCode/verifyEmailCode` 封装。
- **云函数 `seed`**：初始化集合新增 `authCodes`。
- **`miniprogram/README.md`**：部署步骤覆盖 4 个云函数、集合权限建议更新（chats/messages 互读）、SMTP 配置说明、演示 vs 生产边界表更新。

### 技术校验 Verified
- 17 个 JS 文件 `node --check` 全部通过；16 个 JSON 全部合法。

---

## [v0.2.0] - 2026-07-13

小程序 MVP 开发首版：完成微信原生 + 云开发的可运行小程序骨架，覆盖 P0 全流程。

### 新增 Added
- **小程序工程**（`miniprogram/`）：微信原生 + 云开发（`project.config.json` 云开发模式）。
  - **公共层**：`app.js`（云初始化 + 静默登录 + 用户档案）、`app.wxss`（品牌绿主题）、`utils/`（`util` 工具、`db` 数据访问层、`mock` 演示数据）。
  - **数据访问层 `utils/db.js`**：优先云数据库，未接云环境时**自动回退 mock**，保证零配置即可跑通 UI 与流程。
  - **8 个页面**：首页（瀑布流+搜索+分类+下拉刷新）、商品详情、发布（认证拦截+多图上传+违禁词校验）、学生认证（邮箱验证码+学生证两级）、消息列表、聊天会话（**发起交易→双向确认闭环**）、我的交易/我发布的、个人中心。
  - **商品卡片组件** `components/item-card`。
  - **云函数**：`login`（获取 openid）、`seed`（初始化 7 个集合 + 写入演示商品）。
- **小程序说明** `miniprogram/README.md`：目录结构、两种启动方式（零配置 mock / 接入云开发）、演示与生产边界对照、下一步。

### 落地的产品决策 Implemented
- 强认证：发布/发起交易前**拦截未认证用户**，认证走「校园邮箱验证码 + 学生证审核」两级。
- 仅撮合线下当面：聊天页内 `发起交易 → 双方各自确认完成 → 闭环标记已售`，全程不涉及线上支付/货款。
- 安全合规：违禁词校验、举报入口、认证页个人信息合规提示、交易页当面验货防诈骗提示。

### 技术校验 Verified
- 15 个 JS 文件 `node --check` 语法全部通过；14 个 JSON 合法；8 个页面四件套齐全。

---

## [v0.1.0] - 2026-07-12

产品筹备与设计阶段首次交付：完成需求验证物料、MVP 产品设计与原型。

### 新增 Added
- **需求验证问卷**（`docs/需求验证问卷.docx`）：5 大部分共 22 题，覆盖用户背景、二手交易习惯与痛点、强认证接受度、校园导航路书需求、使用与付费意愿；用于阶段 0 需求验证。
- **MVP 产品需求文档 PRD**（`docs/MVP产品需求文档PRD.docx`）：
  - 产品定位与 MVP 边界（In/Out Scope）
  - 用户角色与权限（游客 / 认证学生 / 运营审核 / 超管）
  - 功能清单 26 项，标注 P0/P1/P2 优先级
  - 核心业务流程（学生认证、发布、撮合交易）
  - 页面结构与信息架构、底部导航设计
  - 数据模型概览（User/Item/Chat/Trade 等 9 个核心实体）
  - 非功能需求、技术选型、里程碑排期、风险对策
- **核心页面原型**（`prototypes/prototype.html`）：首页、商品详情、发布、学生认证、聊天、个人中心 6 个页面低保真原型（手机尺寸，浏览器可看）。
- **业务流程图**（`prototypes/flowchart.html`）：学生认证、发布商品、撮合交易、全局页面跳转 4 张 SVG 流程图。
- **文档生成脚本**（`scripts/gen_questionnaire.py`、`scripts/gen_prd.py`）：基于 python-docx 生成 Word 文档，便于后续迭代。
- **项目说明**（`README.md`）：产品定位、交付物清单、目录结构、技术选型、开发路线、合规提示。

### 关键产品决策 Decisions
- 认证强度：**高**，两级门槛（校园邮箱=准入，学生证/学籍=可信身份），仅本校学生可用。
- 交易模式：**仅撮合、线下当面交易**，平台不经手货款、不做资金担保。
- 平台节奏：**第一版微信小程序**，功能完整后再开发安卓 App。
- 导航路书：以 **UGC 图文路书 + 代金券激励** 实现，规避室内实时定位的高成本，列为二期。

---

## 版本规划（后续）

- **v0.4.0**：学生证 OCR + 后台人工审核工作台，消息未读数，完成阶段 0 需求验证（问卷回收 + 结论）。
- **v1.0.0**：小程序正式上线首个目标校（隐私协议、提审、云数据库安全规则与索引）。
- **v2.0.0**：导航路书 + 代金券，安卓 App。
